language: node_js

node_js:
  - '12.15.0'

cache:
  directories:
    - node-modules

sudo: true

addons:
  chrome: stable
  sonarcloud:
    organization: 'deepakjhaaa-github'
    token:
      secure: 'c2600acd88c613fd6da962d4ea806a63d910195d'

before_script:
  - npm install global @angular/cli

script:
  - ng lint
  - sonar-scanner
  - ng test --watch false --browsers ChromeHeadless
  - ng build --prod

before_deploy: "echo 'ready?'"
